@model IEnumerable<MvcApplication1.Models.DBModels.User>

@{
    ViewBag.Title = "UserActivation";
    Layout = "~/Views/Shared/_LayoutSupervisor.cshtml";
}

<h2>UserActivation</h2>

@if (ViewBag.updated != null)
{
    <h3>The user has been activated.</h3>
}

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.UserName)
        </th>
        @*<th>
            @Html.DisplayNameFor(model => model.Password)
        </th>*@
        <th>
            @Html.DisplayNameFor(model => model.FirstName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LastName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Email)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Activated)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.UserName)
        </td>
        
            @Html.HiddenFor(modelItem => item.Password)
        
        <td>
            @Html.DisplayFor(modelItem => item.FirstName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LastName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Email)
        </td>
        <td>
            @*@Html.DropDownListFor(modelItem => item.Activated, (IEnumerable<SelectListItem>)ViewData["activationItems"],"--Select a Value--")*@
            @*@Html.DropDownListFor(modelItem => item.Activated, (SelectList)ViewBag.activateList )*@
            @*@Html.DisplayFor(modelItem => item.Activated)*@
            @*@Html.DropDownListFor(modelItem => item.Activated,(IEnumerable<SelectListItem>)ViewBag.activateList)*@
            
            @*@Html.DropDownListFor(modelItem => item.Activated, new SelectList(
           new List<object>
           {
               new {value=bool.TrueString,text="True"},
               new {value=bool.FalseString,text="False"},
           }
           ))*@

            @Html.DropDownListFor(modelItem => item.Activated, new SelectList(
           new List<object>
           {
               new {value=bool.TrueString,text="True"},
               new {value=bool.FalseString,text="False"},
           },
           "value",
           "text",
           item.Activated
           ),"--Select a Value--")

        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
